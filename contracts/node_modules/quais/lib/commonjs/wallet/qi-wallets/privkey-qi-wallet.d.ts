import { AbstractQiWallet } from './abstract-qi-wallet.js';
import { QiAddressInfo } from '../qi-hdwallet.js';
import { Zone } from '../../constants/zones.js';
export declare class PrivatekeyQiWallet extends AbstractQiWallet {
    private static readonly DEFAULT_GAP_LIMIT;
    constructor();
    /**
     * Imports a private key and adds it to the wallet.
     *
     * @param {string} privateKey - The private key to import (hex string)
     * @returns {Promise<QiAddressInfo>} The address information for the imported key
     * @throws {Error} If the private key or derived address is invalid
     */
    importPrivateKey(privateKey: string): QiAddressInfo;
    getImportedAddresses(zone?: Zone): QiAddressInfo[];
    getAddressesForAccount(account: number): QiAddressInfo[];
    deriveNewAddress(zone: Zone, account?: number): QiAddressInfo;
}
//# sourceMappingURL=privkey-qi-wallet.d.ts.map