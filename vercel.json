{
    "framework": "vite",
    "buildCommand": "cd frontend && npm install && npm run build",
    "installCommand": "echo 'Skipping root install, handled in build command'",
    "outputDirectory": "frontend/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}